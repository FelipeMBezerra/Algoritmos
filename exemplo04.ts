const empresa = "petrobras"; //declação constante
let estado = "Rio de Janeiro"; //declaração de variável

console.log (`O nome da empresa é ${empresa}`);
console.log (`O estado indicado pe ${estado}`);

//empresa = senai
//não funciona

estado = "São Paulo";
console.log(`Agora o estado indicado é ${estado}`);